#define SECOND_FRACTIONS_TO_GRAB 16

// awfuls...
#define MIN(a, b) (((a) < (b)) ? (a) : (b)) 
#define MAX(a, b) ((a) > (b) ? (a) : (b))
extern bool bFirstPacket;
extern REFERENCE_TIME latestGraphStart;
void ShowOutput(const char *str, ...);
